define func()
{
    var x = 3;

    var r = fuck(x);
    print(r);
}


define fuck(new x)
{
    if (x == 1)
    {
        return 1;
    }
    else
    {
        var t = x - 1;
        return x * func(t);
    };
}

