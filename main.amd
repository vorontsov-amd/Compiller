define func(argc, argv, arg3, arg4)
{
    var pivo;
    scanf(pivo, argc);
    var vodka = pivo + 1;
    var sex;
}

define drink(pivo)
{
    pivo = 0;
    printf(pivo);
    exit();
}

define exit()
{
    var x;
}
