define func()
{
    var x = 10;
    var y = 5;
    Translate(x,y);
}

define Translate(x,y)
{
    x = 4;
    y = x + 5;
    var z = (x * y - 3) / 7;
    var c = 0;
    print(x,y,z,c);
}

define print(x, y, z, c)
{

}